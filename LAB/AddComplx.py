class complexx():
 def __init__(self):
  self.real=int(input("Enter the real part : "))
  self.img=int(input("Enter the imaginary part Co-efficient : "))
  self.var=input("Enter the variable of imaginary part : ")

def compAdd(self,other):
 real=self.real+other.real
 img=self.img+other.img
 var=self.var
 print(f"The addition is {real} + {img}{var}")

a=complexx()
b=complexx()
compAdd(a,b)

